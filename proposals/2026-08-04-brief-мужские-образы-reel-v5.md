---
status: approved
applied: 2026-08-04
prompt_id: brief-мужские-образы-reel
created: 2026-08-04
---
# Proposal: v5 — hook снова фраза, а не описание кадра; снапшоты подтянуты к текущим версиям

## Current version
Активна `brief-мужские-образы-reel` v4, `github_path` =
`prompts/briefs/мужские-образы/reel-v4.md`. Дефект в одной строке, раздел
«Задача»:

> - **hook** — по `hook_structure` выбранного рецепта: укладывается в его `timing`
>   и использует хотя бы один элемент из `elements`.

`elements` рецептов этой темы написаны как ОПИСАНИЯ КАДРА, а не как реплики.
`short-styling-idea-reel.hook_structure.elements[0]`: «автор в кадре в своём
актуальном образе с первой секунды, без интро-заставки». Инструкция «использовать
элемент» читается генератором как «перенести элемент в поле», и в поле `hook`
оказывается ремарка вместо фразы, которую слышит зритель.

Предыдущая редакция (`prompts/briefs/мужские-образы/reel.md`, строка 13) такой
регрессии не давала: «**hook** — одна конкретная идея стилизации СЛОВАМИ
(сочетание цветов, приём, «как носить X») ИЛИ вопрос к зрителю о приёме».

Второй, мелкий дефект: пять ссылок на снапшоты в v4 указывают версии, которые
успели смениться (`grwm-interactive-frame-v2` → v3, `short-styling-idea-reel-v4`
→ v6).

## Proposed changes
Кандидат v5: переписан пункт **hook** в разделе «Задача», добавлен запрет ремарки
в поле hook, подтянуты версии снапшотов. Остальной текст сохранён дословно —
правится ровно то, что сломано.

```markdown
# Промпт генерации брифов — мужские-образы / reel (v5)

Ты генерируешь бриф на короткий вертикальный ролик по ОДНОМУ из утверждённых
рецептов темы. Сначала выбирается рецепт — всё остальное выводится из его снапшота.

## Рецепты темы и выбор между ними
Проверяй условия сверху вниз, первое совпадение забирает бриф:

1. **`grwm-interactive-frame` v3** — материал показывает сборы с обращением к
   зрителю (вопрос, выбор, реакция).
   Снапшот: `formulas/_approved/мужские-образы/grwm-interactive-frame-v3.json`.
2. **`reference-recreation-fit` v2** — есть конкретный референс-образ, который
   пересобирается из доступных вещей.
   Снапшот: `formulas/_approved/мужские-образы/reference-recreation-fit-v2.json`.
3. **`personal-rotation-week` v1** — материал про личную ротацию вещей за период
   (что реально носилось несколько дней подряд и почему).
   Снапшот: `formulas/_approved/мужские-образы/personal-rotation-week-v1.json`.
4. **`style-manifesto-statement` v2** — заявление о принципе стиля, а не разбор
   конкретной вещи.
   Снапшот: `formulas/_approved/мужские-образы/style-manifesto-statement-v2.json`.
5. **`short-styling-idea-reel` v6** — базовый случай: короткая идея по стилю.
   Снапшот: `formulas/_approved/мужские-образы/short-styling-idea-reel-v6.json`.

`formulas/мужские-образы/` не читаешь — рабочий черновик может быть новее
утверждённого, и бриф по нему не прослеживается до решения оператора.

## Вход
Снапшот выбранного рецепта из `formulas/_approved/мужские-образы/`
(hook_structure, solution_structure, visual_requirements, cta_type, prohibitions,
evidence).

## Задача
Сгенерируй бриф-JSON по schemas/brief.schema.json:

- **hook** — ФРАЗА, которую зритель слышит или читает в первую секунду. Одна
  конкретная мысль об образе словами, вопрос к зрителю или реплика сценки —
  то, что можно произнести вслух или поставить плашкой.
  `hook_structure.elements` рецепта — это ТРЕБОВАНИЯ К КАДРУ («автор в кадре в
  своём актуальном образе с первой секунды, без интро-заставки»), а не заготовка
  текста. Хук пишется ПО ним, а не копируется ИЗ них: элемент выполняется в
  script и visual_direction, в поле hook уходит только фраза.
  Запрещено класть в hook описание кадра — «0-1с: автор уже в кадре…», «Плашка на
  первом кадре: …», «Первой фразой, автор уже у шкафа…». Это script.
  Длина считается, а не прикидывается: русская речь ≈ 16 знаков в секунду, значит
  в окне `hook_structure.timing` 0-3с помещается ~48 знаков произносимого текста.
  Не помещается — сокращай хук. Ворота (`cf auto-approve`) считают это
  арифметически и не одобрят бриф с ремаркой или непроизносимой длиной.
- **script** — посекундный сценарий по `solution_structure` рецепта: 0–3с хук,
  далее шаги в порядке и длительности, заданных рецептом, финал — резюме приёма
  плюс CTA. Длительность не выходит за рамки, названные рецептом. Реплики внутри
  сегмента `A-Bс` тоже обязаны в него помещаться по той же ставке 16 знаков/с.
- **visual_direction** — как снимать одним креатором на телефон: свет, план,
  локация, смена кадра по `visual_requirements` рецепта, 9:16.
  В конце visual_direction добавь блок «Обязательно: текст-плашка с идеей на
  каждом образе; публикация в окно 9:00–21:00». Бриф без этого блока не отдавать.
- **cta** — по `cta_type` выбранного рецепта, дословно его тип.
- **references** — 1–3 URL из `evidence.source_urls` ТОГО ЖЕ снапшота.
- **source_pattern_ids** — из `source_pattern_ids` снапшота; **formula_id** — его
  `name`; **prompt_version** — версия этого промпта, по которой ты работаешь
  (её выдаёт `cf ab-plan`, не выдумывай).
- **tested_variable** — ось, по которой этот бриф отличается от предыдущих брифов
  той же формулы; **differs_from** — их brief_id. Оба поля описательные: ворота
  всё равно сверяют лексику сценария с одобренными брифами формулы за 30 дней.

## Запреты (нарушение = reject ревьюером)
Сначала — все `prohibitions` выбранного рецепта, они сильнее любого правила ниже.
Сверх них по теме: каталожное перечисление товаров и цен без идеи; простыня
хэштегов вместо смысловой подписи; «артикулы в тг» как единственное содержание;
ролики длиннее 60 секунд; интро или заставка до появления образа в кадре;
пересказ сюжета и реплик референса — из него берётся ПРИЁМ, а не содержание;
описание кадра в поле hook.

## Выход
Строка в CF Creative Briefs: human_status=pending, payload_json = полный бриф-JSON,
плюс поля hook, script_text, visual_direction, reference_url, caption,
prompt_version, formula_id, source_pattern_ids (через запятую) — их читают eval и
trace. Один бриф — один рецепт: смешивать приёмы двух рецептов в одном сценарии
нельзя, иначе результат нельзя отнести ни к одному из них.
```

## Evidence
Сравнение внутри ОДНОЙ темы (мужские-образы) и ОДНИХ четырёх рецептов, общих для
обеих версий (`short-styling-idea-reel`, `grwm-interactive-frame`,
`reference-recreation-fit`, `style-manifesto-statement`) — смешивания тем нет,
сравниваются только промпты:

| версия | брифов | ремарка в поле hook | медиана длины hook |
|---|---|---|---|
| v2 | 29 | 0 (0%) | 71 знак |
| v4 | 8 | 7 (88%) | 186 знаков |

По всем 13 брифам v4 (включая рецепты вне пересечения) ремарка в hook у 12 из 13
(92%), максимум длины 384 знака. Хронология роста длины поля по датам генерации:
10-25.07 — медиана 64 знака, 26-31.07 — 112, 1-3.08 — 170.

Поимённо: `b-short-styling-idea-reel-20260801-season-anchor-caption` (344 знака,
начинается «0-1с: автор уже в кадре целиком в летнем светлом образе…»),
`b-short-styling-idea-reel-20260802-fit-effect-hook` (384),
`b-personal-rotation-week-20260801-tshirt-category` (309),
`b-reference-recreation-fit-20260803-ironic-adaptation`,
`b-grwm-interactive-frame-20260803-field-epilogue`.

Дефект — метаданных, не содержания: у 18 из 20 таких брифов рабочая стартовая
реплика есть внутри script («тридцать градусов — а ты в пиджаке. Честно: жарко?»).
Но поле `hook` читают eval и trace, и на воротах оно теперь считается.

Первоисточники: `prompts/briefs/мужские-образы/reel-v4.md` (строка 35 — дефектная
формулировка), `prompts/briefs/мужские-образы/reel.md` (строка 13 — редакция без
регрессии), `formulas/_approved/мужские-образы/short-styling-idea-reel-v6.json`
(hook_structure.elements как описания кадра), полный разбор —
`agent-runtime/analysis/2026-08-04-script-quality-audit.md` и
`agent-runtime/analysis/2026-08-04-quantitative-findings.md`.

Отдельно о том, почему v4 вообще стала активной: eval 2026-07-27 записал «версия
промпта v4 бьёт v2 в параллельной когорте (14600 против 5100) — производить по
v4». Обе цифры — из строк `cf demo-seed`; отчёт сам помечен
`dataset.provenance_warning`: «Все 35 замеренных роликов — строки демо-прогона».
Боевых замеров в системе 0 (46/46 строк CF Published Reels и 48/48 CF Performance
несут метку DEMO-). То есть действующая версия выбрана по выдуманным числам, и
единственный реальный сигнал против неё — этот дефект поля.

## Confidence
Высокая по факту дефекта: 12/13 против 0/29 — не шум, а регрессия одной строки
промпта, и корень виден в тексте обеих редакций.

Средняя по тому, что v5 окажется ЛУЧШЕ v4 по результату у аудитории: результата
пока нет ни у одной версии, сравнивать нечем. Поэтому правка идёт кандидатом в
A/B, а не заменой активной версии, — вердикт вынесет первый eval на боевых
замерах.

## Risks
- **Кандидат чинит форму, а не идею.** Хук станет произносимым; станет ли он
  интереснее — не гарантировано и этой правкой не проверяется.
- **A/B не разрешится, пока нет боевых замеров.** Пара v4/v5 будет чередоваться
  вслепую до первой реальной публикации; это осознанная цена честного
  interleaving (P5.13) — альтернатива была бы решением по демо-данным, которое
  уже один раз выбрало v4.
- **Ужатие хука до ~48 знаков может обеднить длинные форматы** (personal-rotation-week,
  personal-style-system с окнами 0-5с и 0-8с). Ставка 16 знаков/с применяется к
  окну КОНКРЕТНОГО рецепта, а не к фиксированным 3 секундам, — для окна 0-8с это
  ~128 знаков.
- **Пять ссылок на снапшоты подтянуты к сегодняшним версиям** и снова устареют при
  следующем ре-апруве рецепта. Это общий долг промптов тем, здесь он не решается.
