# 06 — Деплой, ротация, доки, eval-промпт

**What to build:** Контур становится жильцом прода. systemd-пара
`cf-collect-metrika.{service,timer}` (ежедневно 08:20, между raw 08:00 и
performance 08:40) по образцу существующих, строка в подсказке
`deploy/install.sh`. CF UTM Traffic добавляется в ротацию `cf archive` по
дневной дате (месячные строки-биллинг не ротируются); CF Orders не
ротируется никогда. В RUNBOOK — раздел «UTM-контур»: как завести токен
Метрики (имя файла секрета, права), counter_id, галка e-commerce в КИТе,
заполнение `accounts`. Промпт eval-агента узнаёт о новых полях датасета НЕ
прямой правкой (deny-правило №3): черновик изменения в `proposals/` +
применение аппликатором; если применить машинно-проверенным путём нельзя —
proposal остаётся лежать с перечнем причин, команда для владельца — одной
строкой в отчёте.

**Blocked by:** 05 (финальная форма полей датасета для промпта).

**Status:** done

- [x] Юниты валидны (структурная проверка в тесте по образцу существующих),
  таймер в 08:20, `install.sh` подсказывает включение.
- [x] `cf archive` ротирует дневные строки CF UTM Traffic старше порога и не
  трогает месячные и CF Orders; тест на партиционирование.
- [x] RUNBOOK-раздел перечисляет все PLACEHOLDER-шаги владельца (токен,
  counter_id, e-commerce галка, accounts) — только имена файлов/ключей, без
  значений.
- [x] Proposal для eval-промпта лежит в `proposals/`, применён аппликатором
  или оставлен с причинами, почему машинный путь недоступен.
- [x] Полный сьют зелёный.

## Комментарии

2026-07-30 — сделано, +6 тестов (юниты 4 в новом `tests/test_deploy_units.py`,
включая «08:20 строго между raw и performance»; архив 2), полный сьют
1910 passed. Ротация CF UTM Traffic обошлась без спец-кода: пара
`("utm_traffic", "date")` в `ARCHIVE_TABS` — месячные строки несут `date=""`
и попадают под существующее правило «недатированную не архивируем никогда»
(`_row_archive_date` → None), тест закрепляет, что партиция идёт по `date`, а
не по `collected_at`; CF Orders в списке нет, и это тоже держит тест. RUNBOOK:
раздел §6 «UTM-контур — приёмка владельцем» (7 шагов, только имена
файлов/ключей) + строка токена Метрики в таблице секретов §1. Proposal
`proposals/2026-07-30-eval-agent-money-fields.md` ОСТАВЛЕН лежать
(status: proposed): аппликатор `cf apply-agent-edit` существует, но при
применении пишет строку в CF Run Log (Google Sheets) и делает собственный
git-коммит — из-под агента такой запуск запрещён рамками тикета. Черновик
сверен локально read-only функциями (`validate_proposal_text` — OK;
`parse_draft`/`_check_edit` — OK; правка = чистая вставка 25-строчной секции,
остальной текст промпта байт-в-байт). Команда владельцу:
`.venv/bin/python -m cf apply-agent-edit 2026-07-30-eval-agent-money-fields.md`.
Чужие слои (staged codex-sync +35 и незастейдженный hiring +130 в
`src/cf/cli.py`, hiring-ханк в `deploy/install.sh`) в коммит не взяты —
развод слоёв со сверкой диффов, как в 02–05; `cf.config.json` не тронут.
