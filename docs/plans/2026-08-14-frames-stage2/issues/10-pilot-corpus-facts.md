# 10 — Пилот: факты по всему корпусу выбранным капом

**Blocked by:** 09
**Status:** done
**Type:** enhancement

Спека: docs/plans/2026-08-14-frames-stage2/spec.md (§План пилота, п. 2).
Блокер содержательный: нужен вердикт оператора по капу (тикет 09 закрыт =
`vision.frames_cap` стоит в конфиге).

## Что построить

Кадровые факты по всему корпусу пилота: все 42 строки (32 TikTok + 10 IG,
1077 кадров) выбранным капом, JSONL-режим (`--jsonl`) — живые Sheets не
трогаются. Прогон(ы) — в Run Log с `--started-at`.

Этот корпус фактов — материал слепого анализа (тикет 11): по нему пойдут
прогоны «факты обложек» против «фактов с кадрами». Отказы движка по
отдельным строкам не чинятся молча: сколько разобрано, сколько отказов и
почему — в короткую заметку к прогону.

## Критерии приёмки

- [ ] Все 42 строки корпуса прогнаны выбранным капом в JSONL-режиме; выход
      валиден схемой кадрового режима.
- [ ] Прогоны в Run Log; заметка с числами (разобрано/отказов/причины) в
      `agent-runtime/analysis/`.
- [ ] Строки с `frames_analyzed` меньше поданного перечислены в заметке
      (материал аудита честности).

## Комментарии

2026-08-14 (вечер): все 42 строки корпуса разобраны капом 30 (флагом,
конфиг не тронут): done=42, failed=0, строк с frames_analyzed меньше
поданного — ноль. Сводный файл:
agent-runtime/collect/frames-pilot/corpus-cap30-visual.jsonl; заметка:
agent-runtime/analysis/2026-08-14-frames-corpus-facts.md. Прогоны в Run Log.
