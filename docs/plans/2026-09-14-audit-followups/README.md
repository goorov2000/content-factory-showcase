# Хвосты аудита 14.09.2026

Аудит репозитория 14.09.2026 (27 агентов: 7 разведчиков, 8 областей код-ревью, по адверсарному
проверяющему на каждую область, панель из трёх позиций о разделении и итог). Из 49 находок подтверждены 44,
опровергнуты 5. Исправленное лежит в коммитах `agency:`/`factory:`/`infra:` от 14.09 с тестами. Здесь то,
что осталось, — чтобы оно жило в репозитории, а не только в отчёте
(опубликованный артефакт; в витрине отсутствует).

## Тикеты

| № | Что | Статус |
| --- | --- | --- |
| [01](issues/01-formula-guard-remembers-auto-pause.md) | Машина помнит рецепт, который сама сняла с производства | отложено до включения завода |
| [02](issues/02-readme-rewrite.md) | README.md под нынешнюю систему | ready-for-agent |
| [03](issues/03-vps-runbook.md) | Аварийный RUNBOOK под VPS | ready-for-agent |

## Подтверждённые и не исправленные находки

Все проверены отдельным агентом, который пытался их опровергнуть; серьёзность — после проверки.

| Где | Что | Серьёзность | Почему не исправлено |
| --- | --- | --- | --- |
| `src/cf/dashboard/runner.py:1851-1914` | Файл доработки есть, но `cf revise-brief` его не применил (схема, brief_id, сбой Sheets) — бриф всё равно помечается «фиксер отказался». Сбой самого агента исправлен 14.09, этот случай — нет | medium | нужна отдельная пометка «не применилась» и ограничение повторов |
| `tests/fakes.py:54, 421-522` | Фейковая таблица не повторяет `gspread.utils.numericise`: баги «текст стал числом» тесты не видят (так жил 500 на `/briefs`) | medium | правка фейка затрагивает сотни тестов |
| `src/cf/sheets.py:133-173, 228-236` | Проекция `read_rows` склеивает имена колонок из кэша (TTL 300 с) со значениями живого листа — при вставке колонки в середину raw-вкладки значения съезжают | low | триггер — ручная вставка колонки в окне 5 минут |
| `src/cf/dashboard/autogate.py:58-69` | Опечатка в `gates.policy` молча оставляет ворота на автомате; `GATE_KEYS` нигде не используется | low | тест закрепляет «мусор → auto» для значения; нужна только проверка ключей |
| `src/cf/dashboard/runner.py:1515-1556` | Воркер «Проверка рецептов» зовёт платного судью, даже когда `gates.policy.recipes = manual` (воркер промпта политику проверяет) | low (не перепроверено) | найдено проверяющим, отдельно не верифицировано |
| `src/cf/cli.py:1933-1945`, `schemas/brief.schema.json` | Фиксер не может переписать капшен, хотя craftcheck его судит | low | правка задевает промпт `brief-fixer` — только через `cf apply-agent-edit` |
| `src/cf/dashboard/runner.py:2703-2704` | Автопродолжение молча не запускается, если не прочиталась вкладка версий промптов | low | — |
| `src/cf/collect/instagram.py:344-358` | Рилсы без речи оплачиваются на расшифровку каждый день до 30 дней: неудачная попытка не запоминается | low | нужна колонка или статус попытки — решение по схеме листа |
| `src/cf/dashboard/decisions.py:55-63`, `prompt_apply.py:333` | Коммит решения с дашборда делает `git add` целыми каталогами и забирает чужие незакоммиченные файлы | low | передавать точные пути, которых коснулось решение |
| `src/cf/cli.py:1078-1083` | `cf auto-approve` не проверяет свежесть вердикта ревьюера: вчерашний recommend может одобрить переписанный сегодня текст (у рецептов такая проверка есть, `runner.py:1597-1631`) | medium (не перепроверено) | найдено проверяющим |
| `src/cf/craftcheck.py:140-159` | Правило «криэйтор подставляет» в одном поле снимает проверку подстановок со всех полей брифа | low | меняет калиброванный порог — решение по данным |
| `src/cf/vision.py` запись в лист | Исключение Sheets при записи после оплаченного вызова вылетает без строки в Run Log | low | — |
| `src/cf/collect/performance.py:85-126` | Дозамер без окна по возрасту: каждый опубликованный ролик замеряется платным раном каждый день бессрочно, eval берёт один замер около 7-го дня | low | при включении завода |
| CF Raw TikTok, `tiktok_7657940825103469856` | В `transcript_text` лежит страница ошибки Varnish 403 (эпоха n8n) | данные | запись в живой лист — оператор |
