# Акт приёмки этапа 4 — параллельный прогон Python-сбора против n8n

Дата: 2026-07-24 (ночной прогон 01:27–02:28 UTC+3). Инструменты:
`cf collect <stage> --dry-run --batches 0 --no-explore` (полный сбор без
записи в Sheets) и `cf collect-diff <stage>` (src/cf/collect/parity.py).
JSON-отчёты и логи — `agent-runtime/collect/` (не версионируются).

## 1. Параллельный прогон, дифф строк листа

Эталон — последние боевые раны n8n: tiktok/instagram 23.07 ~04:03/04:22 UTC,
snowball (еженедельный) 19.07. Разрыв запусков 19–114 ч — счётчики просмотров
законно дрейфуют; смысловая проверка порта — точные поля.

| Тракт | python | n8n | общих | Точные поля* | Капшены | Транскрипты |
| --- | --- | --- | --- | --- | --- | --- |
| tiktok | 191 (6/6 батчей) | 213 | 67 | 100% | 65/65 | 31/31 равны |
| instagram | 30 (reel 30/30) | 30 | 13 | 100% | 13/13 | см. п.3 |
| snowball | 42 (4 сида) | 22 | 8 | 100% | 8/8 | 3/3 равны |
| performance | insufficient_data | пусто | — | контракт совпал: реелов нет, обеим сторонам нечего замерять | | |

*точные поля: url, author, video_id, created_at, is_ad, duration_sec, platform.
Расхождения точных полей — 0 на всех трактах.

Дрейф счётчиков: медианы 6–13% (растут со временем разрыва), выбросы до
1300% — виральные ролики; engagement_rate медианно ≤0.5%. Состав строк
(«только python» / «только n8n») объясняется окном PAST_WEEK/LATEST на
разное время суток и разным составом батчей — источники совпадают.

## 2. Битый батч

Драйвер agent-runtime/collect/broken-batch-test.py: первый вызов актора
искусственно падает. Результат: `batches=2 batches_failed=1 sources_lost=4`,
второй батч доехал (17 строк, 5 субтитров), потеря честно видна в сводке и
CF Run Log. Контракт деградации совпадает с n8n (onError: continue).

## 3. Особое внимание Instagram (живые раны)

- Контракт reel-scraper (reel-URL в поле `username`) работает: 30/30 реелов.
- Discovery жив (16 хэштегов, discovery_failed=False), гейт 51/160 до
  дорогого reel-скрейпа.
- Транскрипты общих реелов совпали 2/11 — расхождение объяснено:
  reel-scraper распознаёт звук заново при каждом ране (ASR), тексты дрейфуют
  между ЛЮБЫМИ двумя ранами, включая два рана самого n8n («palms» vs «arms»,
  «Мейси» vs «Мэйси», разная длина). Это недетерминизм актора, не порта;
  тракт по контракту «перечитывает транскрипт каждым сбором». Покрытие у
  Python не хуже (у 1 реела транскрипт есть только у Python).

## 4. Регрессия

- pytest: 1050 passed (полный прогон).
- node --test 'n8n/__tests__/*.test.js': 145/145 (parity-карта JS-эталона).

## Вердикт

Инструментальные критерии этапа 4 зелёные: расхождения разобраны до нуля
объяснимых. Остаётся ревью оператора (чекбокс 4 гайда) и команда на этап 5
(таймеры CF вкл, расписания n8n выкл, неделя наблюдения).
