[Unit]
Description=CF: сторож завода (ежедневно, после утреннего цикла)

[Timer]
# Цикл стартует в 07:30 и укладывается примерно в час; сбор статистики —
# в 08:40. 11:00 — заведомо после всего, но ещё утро.
OnCalendar=*-*-* 11:00:00
Persistent=true

[Install]
WantedBy=timers.target
