---
name: cf-classify-niche
description: "Niche Classifier - определить ниши raw-видео по контенту (или ре-аудит, или предложить новые ниши из кластеров)"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-classify-niche.md — не править руками -->

Codex-обёртка команды завода `cf-classify-niche`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-classify-niche.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-classify-niche.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: [tab=raw_tiktok] [--reaudit [ниша]] [--clusters <tab>]).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
