---
name: cf-tune-sources
description: "Source Tuner - скоринг источников сбора, proposal на обновление списков"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-tune-sources.md — не править руками -->

Codex-обёртка команды завода `cf-tune-sources`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-tune-sources.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-tune-sources.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу.
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
