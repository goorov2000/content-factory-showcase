---
name: cf-formula
description: "Formula Writer - вывести формулу из валидированных паттернов"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-formula.md — не править руками -->

Codex-обёртка команды завода `cf-formula`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-formula.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-formula.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: [путь к patterns-файлу] [pattern_id ...]).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
