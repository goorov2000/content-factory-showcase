---
name: cf-status
description: "Content Factory - запуски, pending-ревью, proposals, активные промпты"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-status.md — не править руками -->

Codex-обёртка команды завода `cf-status`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-status.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-status.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу.
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
