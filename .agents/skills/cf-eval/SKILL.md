---
name: cf-eval
description: "Eval Agent - еженедельная оценка performance по prompt_version"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-eval.md — не править руками -->

Codex-обёртка команды завода `cf-eval`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-eval.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-eval.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: [--since YYYY-MM-DD]).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
