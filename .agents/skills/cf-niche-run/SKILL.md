---
name: cf-niche-run
description: "Niche Pipeline - профиль + анализ + черновики формул одной ниши (для фан-аута)"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-niche-run.md — не править руками -->

Codex-обёртка команды завода `cf-niche-run`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-niche-run.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-niche-run.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: <tab> <niche>).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
