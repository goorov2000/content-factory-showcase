---
name: cf-propose-update
description: "Prompt Optimizer - evidence-backed proposal изменения промпта"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-propose-update.md — не править руками -->

Codex-обёртка команды завода `cf-propose-update`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-propose-update.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-propose-update.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: <prompt_id>).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
