---
name: cf-profile
description: "Raw Batch Profiler - проверить качество батча перед анализом"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-profile.md — не править руками -->

Codex-обёртка команды завода `cf-profile`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-profile.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-profile.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: [tab=raw_tiktok] [--niche X] [--since YYYY-MM-DD]).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
