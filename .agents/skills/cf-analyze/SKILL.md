---
name: cf-analyze
description: "Pattern Analyzer - анализ паттернов профилированного батча"
---

<!-- сгенерировано `cf codex-sync` из .claude/commands/cf-analyze.md — не править руками -->

Codex-обёртка команды завода `cf-analyze`. Канонический текст команды один для всех
агентов — `.claude/commands/cf-analyze.md`; здесь он не дублируется, чтобы не плодить
источник дрейфа.

Порядок:
1. Прочитай `.claude/commands/cf-analyze.md` и выполни его инструкции от корня репозитория.
2. `$ARGUMENTS` в тексте команды — аргументы, переданные этому скиллу (подсказка формата: [tab=raw_tiktok] [--niche X] [--since YYYY-MM-DD]).
3. CLI завода — `.venv/bin/python -m cf ...`; правила — AGENTS.md.
